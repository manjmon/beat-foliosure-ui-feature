import { TruncateFilePipe } from './truncate-file.pipe';

describe('TruncateFilePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateFilePipe();
    expect(pipe).toBeTruthy();
  });
});
